'use strict';

require('dotenv').config();

module.exports = {
  // Токен бота берётся из .env
  TOKEN: process.env.DISCORD_TOKEN,

  // Опционально: ID сервера для мгновенной регистрации слэш-команд.
  // Если пусто/undefined — команды регистрируются глобально.
  GUILD_ID: process.env.GUILD_ID || null,

  // Якорная роль: новая роль комнаты создаётся прямо над ней в иерархии.
  ANCHOR_ROLE_ID: '1507662794387095562',

  // Триггер-канал (join-to-create): при входе сюда создаётся/открывается комната.
  TRIGGER_CHANNEL_ID: '1513276381025271808',

  // Префикс к названию голосового канала комнаты, например: ・room1
  ROOM_PREFIX: '・',

  // Цвет роли по умолчанию, если пользователь указал некорректный цвет.
  DEFAULT_COLOR: 0x5865f2,
};
